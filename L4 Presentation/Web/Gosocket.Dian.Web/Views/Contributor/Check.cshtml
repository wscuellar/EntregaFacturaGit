@model Gosocket.Dian.Web.Models.ContributorViewModel

@{
    ViewBag.Title = "Registrar facturador";
}

<div class="page-content margin-15">
    <div class="row margin-horizontal-0">
        <div class="panel">
            <div class="panel-body padding-top-20">
                <div class="row margin-horizontal-0">
                    <div id="panel-form" data-target="#panel-form" data-toggle="panel-overlay" class="panel panel-bordered-default margin-bottom-0">
                        @*@using (Html.BeginForm("Register", "Contributor", FormMethod.Post, new { id = "register-contributor-form" }))
                        {
                            @Html.Partial("_form")
                            <div class="panel-footer text-right">
                                <button type="button" class="btn btn-default btn-hover-gosocket btn-register">Registrar</button>
                            </div>
                        }*@
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    @*@Scripts.Render("~/bundles/jqueryval")
    <script src="~/Content/js/contributor/contributor.js?1.0.0"></script>*@

    @*<script>
        $(document).ready(function ($) {

            $('#Code').attr('readonly', true);

            if ('@Model.OperationModeId' === '2')
                $('#software').css('display', '');
            if('@Model.OperationModeId' === '3')
                $('#providers').css('display', '');

            $('#OperationModeId').change(function () {
                if ($(this).val() === "2")
                    $('#software').css('display', '');
                else
                    $('#software').css('display', 'none');

                if ($(this).val() === "3")
                    $('#providers').css('display', '');
                else
                    $('#providers').css('display', 'none');
            });

            $('.btn-register').click(function () {
                var form = $('#register-contributor-form');
                if (form.valid()) {
                    showLoading('#panel-form', 'Registering', 'Processing data, please wait.');
                    form.submit();
                }
            });
        });

        function cleanFields() {
            $('#Name').val('');
            $('#BusinessName').val('');
            $('#Email').val('');
        }

    </script>*@
}